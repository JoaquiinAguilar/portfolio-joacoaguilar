<%- include('partials/header') %>

<section class="error-section">
    <div class="container">
        <div class="error-content">
            <div class="error-icon">
                <i class="fas fa-exclamation-triangle"></i>
            </div>
            <h1>404</h1>
            <h2><%= language === 'en' ? 'Page Not Found' : 'Página No Encontrada' %></h2>
            <p>
                <% if(language === 'en') { %>
                    The page you are looking for might have been removed, had its name changed, or is temporarily unavailable.
                <% } else { %>
                    La página que estás buscando puede haber sido eliminada, ha cambiado de nombre o no está disponible temporalmente.
                <% } %>
            </p>
            <a href="/" class="btn primary">
                <i class="fas fa-home"></i> <%= language === 'en' ? 'Back to Home' : 'Volver al Inicio' %>
            </a>
        </div>
    </div>
</section>

<style>
    .error-section {
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: calc(100vh - 4.5rem - 15rem);
        text-align: center;
    }
    
    .error-content {
        max-width: 600px;
        padding: 3rem;
    }
    
    .error-icon {
        font-size: 4rem;
        color: var(--text-secondary);
        margin-bottom: 1rem;
    }
    
    .error-content h1 {
        font-size: 6rem;
        font-weight: 800;
        margin: 0;
        line-height: 1;
        color: var(--accent-primary);
    }
    
    .error-content h2 {
        margin-bottom: 1.5rem;
    }
    
    .error-content p {
        margin-bottom: 2rem;
        color: var(--text-secondary);
    }
</style>

<%- include('partials/footer') %>